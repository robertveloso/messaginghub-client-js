{
    "port": 8124,
    "name": "Iris",
    "protocol": "tcp",
    "stubs": [
        {
            "predicates": [{ "inject": "<%- stringify(filename, 'isAuth.js') %>" }],
            "responses": [{ "inject": "<%- stringify(filename, 'authResponse.js') %>" }]
        },
        {
            "predicates": [{ "inject": "<%- stringify(filename, 'isCommand.js') %>" }],
            "responses": [{ "inject": "<%- stringify(filename, 'commandResponse.js') %>" }]
        },
        {
            "predicates": [{ "inject": "<%- stringify(filename, 'isEvent.js') %>" }],
            "responses": [{ "inject": "<%- stringify(filename, 'eventResponse.js') %>" }]
        },
        {
            "predicates": [{ "inject": "<%- stringify(filename, 'isMessage.js') %>" }],
            "responses": [{ "inject": "<%- stringify(filename, 'messageResponse.js') %>" }]
        }
    ]
}
